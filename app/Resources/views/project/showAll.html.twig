{% extends 'base.html.twig' %}

{% block projects %}
    <div>
        {% for project in projects %}
            <div>
                <a href="{{ path('project_show', {'id': project.id} )  }}">{{ project.name }}</a>
            </div>

        {% endfor %}
    </div>
{% endblock %}